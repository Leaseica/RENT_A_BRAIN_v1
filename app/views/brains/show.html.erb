<div class="card-trip">
  <%= image_tag @brain.photo %>
  <div class="card-trip-infos">
    <div>
      <h2><%= @brain.title %> - <%= @brain.specialty %></h2>
      <p><%= @brain.description %></p>
      <p><%= @brain.address %></p>
      <p><%= link_to @brain.wiki_page, @brain.wiki_page %></p> <!-- Adjusted for clickable link -->
    </div>
    <h2 class="card-trip-pricing"><%= @brain.price %> €</h2>
    <%= link_to "Back to the list", brains_path, class: "btn btn-secondary" %> <!-- Bootstrap class for styling -->
    <%= link_to "Make a Reservation", new_brain_reservation_path(@brain), class: "btn btn-primary" %> <!-- Add reservation link -->
  </div>
</div>
